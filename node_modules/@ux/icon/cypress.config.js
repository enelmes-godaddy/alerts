const { config, devServer, setupNodeEvents } = require('@ux/cypress-config');
const { defineConfig } = require('cypress');

module.exports = defineConfig({
  ...config,

  component: {
    devServer,
    setupNodeEvents: (...args) => setupNodeEvents(...args),
    excludeSpecPattern: '**/node_modules/**',
    video: false
  }
});

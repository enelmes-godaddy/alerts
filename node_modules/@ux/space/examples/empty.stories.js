import space, { asOptions } from '../src';
import React from 'react';

export { default as default } from './index.stories.js';

export function Empty() {
  return (
    <>
      <p>Same as default examples, as spacer-gifs</p>
      <space.div className='example' inline block as={ asOptions.MODULE }/>
      <space.div className='example' inline block as={ asOptions.BLOCK }/>
      <space.div className='example' inline block as={ asOptions.LOCKUP }/>
    </>
  );
}
